{
  "name": "viaeats-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --concurrency=1",
    "build:web": "pnpm --filter web build",
    "build:admin": "pnpm --filter admin build",
    "build:api": "pnpm --filter @viaeats/api build",
    "launch:verify-live": "node tools/verify-launch-live.mjs",
    "db:migrate": "node -e \"console.error('BLOCKERAD: migreringshistoriken är inte baselined mot produktion. Följ docs/LAUNCH_DATABASE_RUNBOOK.md.'); process.exit(1)\"",
    "db:validate": "pnpm --filter @viaeats/api exec prisma validate --schema prisma/schema.prisma",
    "db:status": "pnpm --filter @viaeats/api exec prisma migrate status --schema prisma/schema.prisma",
    "db:seed": "pnpm --filter db seed",
    "db:studio": "pnpm --filter db studio"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "supportedArchitectures": {
      "os": [
        "linux",
        "darwin",
        "win32"
      ],
      "cpu": [
        "x64",
        "arm64"
      ],
      "libc": [
        "glibc",
        "musl"
      ]
    },
    "onlyBuiltDependencies": [
      "sharp",
      "@prisma/client",
      "prisma",
      "@prisma/engines",
      "@sentry/cli",
      "esbuild",
      "@biomejs/biome",
      "core-js"
    ]
  }
}
