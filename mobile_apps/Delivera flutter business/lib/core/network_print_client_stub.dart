class NetworkPrintClient {
  static Future<bool> sendBytes({
    required String host,
    required int port,
    required List<int> bytes,
    required int copies,
  }) async {
    return false;
  }
}
