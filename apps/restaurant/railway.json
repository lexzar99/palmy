{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm build --filter=restaurant"
  },
  "deploy": {
    "startCommand": "pnpm start --filter=restaurant",
    "rootDirectory": "./"
  }
}
